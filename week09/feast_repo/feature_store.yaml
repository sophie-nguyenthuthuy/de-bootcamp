# Week 9 Buổi 17 — Feast feature store (Redis online)
# Chạy từ thư mục feast_repo: feast apply
project: de_bootcamp
registry: data/registry.db
provider: local

online_store:
  type: redis
  connection_string: "localhost:6379"

# Offline store mặc định (file) cho development
offline_store:
  type: file
